[package]
name = "prometheus-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
spin = "0.9"

[features]
baseline = []
user_repeat = []
exc_ud = []
exc_gp = []
exc_pf = []
exc_df = []

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
